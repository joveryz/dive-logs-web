/**
 * 通用类型定义
 * @module types/common
 */

// ============================================================================
// 数值范围类型
// ============================================================================

/**
 * 数值范围
 */
export interface NumberRange {
  min: number;
  max: number;
}

/**
 * 排序方向
 */
export type SortDirection = 'asc' | 'desc';
